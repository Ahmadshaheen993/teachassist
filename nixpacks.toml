[phases.setup]
nixPkgs = ["nodejs_22", "pnpm", "libreoffice", "fontconfig"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = ["pnpm build", "fc-cache -f"]

[start]
cmd = "node dist/index.js"
