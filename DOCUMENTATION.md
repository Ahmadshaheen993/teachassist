# توثيق منصة مساعد المعلم

## نظرة عامة

منصة ويب متكاملة لمساعدة معلمي المدارس في منطقة الخليج العربي على توليد خطط الدروس اليومية وأوراق العمل بشكل تلقائي عبر الذكاء الاصطناعي (Claude API)، وفق النماذج الوزارية المعتمدة، مع نظام اشتراكات ودفع إلكتروني وإحالات ومحتوى تربوي داعم.

---

## البنية التقنية

| المكون | التقنية |
|--------|---------|
| الواجهة الأمامية | React 19 + TypeScript + TailwindCSS 4 |
| البنية الخلفية | Express 4 + tRPC 11 |
| قاعدة البيانات | MySQL/TiDB (عبر Drizzle ORM) |
| المصادقة | Manus OAuth |
| الذكاء الاصطناعي | Claude API (عبر Manus Forge) |
| التخزين | S3 (للملفات المستقبلية) |
| الاختبار | Vitest (17 اختباراً) |

---

## قاعدة البيانات (21 جدولاً)

### جداول المناهج
| الجدول | الوصف |
|--------|-------|
| `countries` | الدول المدعومة مع الأسعار والعملات |
| `stages` | المراحل الدراسية (ابتدائي، إعدادي، ثانوي) |
| `grades` | الصفوف الدراسية مرتبطة بالمراحل |
| `subjects` | المواد الدراسية مرتبطة بالدول |
| `schools` | المدارس (مع نظام توثيق) |
| `terms` | الفصول الدراسية |
| `textbooks` | الكتب المدرسية |
| `units` | وحدات الكتاب |
| `lessons` | الدروس مع الأهداف والصفحات |

### جداول المستخدم
| الجدول | الوصف |
|--------|-------|
| `users` | المستخدمون (مع دور admin/user) |
| `saved_selections` | التركيبات المحفوظة (دولة/صف/مادة) |

### جداول التوليد
| الجدول | الوصف |
|--------|-------|
| `plan_templates` | قوالب التحضير الوزارية لكل دولة |
| `plans` | الخطط المولّدة (مع كاش) |
| `worksheets` | أوراق العمل المولّدة (مع كاش) |

### جداول الاشتراكات
| الجدول | الوصف |
|--------|-------|
| `purchases` | سجل المشتريات |
| `plan_credits` | رصيد الخطط الفردية |
| `subscriptions` | الاشتراكات الفصلية |

### جداول الإحالات
| الجدول | الوصف |
|--------|-------|
| `referral_codes` | أكواد الإحالة |
| `referral_redemptions` | استخدامات الأكواد |
| `referral_rewards` | المكافآت الممنوحة |

### جداول المحتوى
| الجدول | الوصف |
|--------|-------|
| `resources` | الموارد التربوية (مواقف، روابط، تقاويم، نماذج) |

---

## الميزات المنفذة

### 1. صفحة توليد خطة الدرس
- اختيار متسلسل: الدولة ← المرحلة ← الصف ← المادة ← الكتاب ← الوحدة ← الدرس
- توليد الخطة عبر Claude API مع JSON Schema صارم
- نظام كاش: نفس الدرس لا يُعاد توليده
- عرض الخطة بتنسيق أنيق (أهداف بلوم، استراتيجيات، خطوات التنفيذ، التقويم الثلاثي)
- توليد ورقة عمل اختيارية مرتبطة بالخطة

### 2. تصدير PDF و Word
- **PDF**: توليد HTML منسق وفتحه في نافذة طباعة منفصلة
- **Word**: تصدير HTML كملف `.doc` قابل للفتح في Microsoft Word

### 3. نظام الاشتراكات والرصيد
- خطة فردية (لكل خطة درس)
- اشتراك فصلي (غير محدود طوال الفصل)
- بوابات دفع: MyFatoorah و Tap (هيكل جاهز للتكامل)
- خصم الرصيد فقط للمستخدمين بدون اشتراك نشط

### 4. نظام الإحالات
- إنشاء أكواد إحالة فريدة
- تتبع عدد الاستخدامات والاشتراكات المدفوعة
- مكافأة فصل مجاني عند 5 اشتراكات مدفوعة

### 5. الموارد التربوية
- مواقف تربوية
- روابط مهمة
- تقاويم
- نماذج رسمية
- تصفية حسب الدولة

### 6. لوحة تحكم المعلم
- نظرة عامة على الإحصائيات
- أحدث الخطط المولّدة
- حالة الاشتراك والرصيد
- سجل المشتريات

### 7. لوحة تحكم المدير
- إدارة الدول والمراحل والصفوف
- إضافة كتب ووحدات ودروس
- مراجعة المدارس غير الموثقة وتوثيقها

### 8. التصميم
- دعم كامل للغة العربية واتجاه RTL
- خط Tajawal العربي
- لوحة ألوان فيروزية/خضراء أنيقة مناسبة للبيئة التعليمية
- تأثيرات حركية ناعمة (hover, transitions)
- تصميم متجاوب (mobile-first)

---

## هيكل الملفات الرئيسي

```
teacher-assistant/
├── client/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Home.tsx          # لوحة المعلم الرئيسية
│   │   │   ├── GeneratePlan.tsx  # توليد خطة درس
│   │   │   ├── MyPlans.tsx       # سجل الخطط وأوراق العمل
│   │   │   ├── Selections.tsx   # الاختيارات المحفوظة
│   │   │   ├── Resources.tsx     # الموارد التربوية
│   │   │   ├── Referrals.tsx    # الإحالات
│   │   │   ├── Subscription.tsx  # الاشتراك والدفع
│   │   │   └── Admin.tsx         # لوحة المدير
│   │   ├── components/
│   │   │   └── DashboardLayout.tsx
│   │   ├── App.tsx
│   │   └── index.css
│   └── index.html
├── server/
│   ├── routers.ts               # tRPC procedures
│   ├── db.ts                    # استعلامات قاعدة البيانات
│   └── teacher-assistant.test.ts
├── drizzle/
│   └── schema.ts                # مخطط قاعدة البيانات
└── todo.md
```

---

## الميزات المستقبلية المقترحة

1. **تكامل فعلي مع MyFatoorah و Tap**: يتطلب مفاتيح API من البوابات
2. **تصدير Word احترافي**: استخدام docxtemplater مع قوالب وزارية جاهزة
3. **إضافة المزيد من الدول**: السعودية، الإمارات، الكويت، البحرين، عمان
4. **نظام إشعارات**: تنبيهات للمستخدمين عند انتهاء الاشتراك
5. **تطبيق جوال**: React Native / Expo

---

## تشغيل المشروع

```bash
# تثبيت الحزم
pnpm install

# تشغيل بيئة التطوير
pnpm dev

# بناء المشروع
pnpm build

# تشغيل الاختبارات
pnpm test
```

---

## الاختبارات

تم كتابة 17 اختباراً تغطي:
- استعلامات المناهج (الدول، المراحل، الصفوف، الكتب، الدروس)
- حفظ الاختيارات
- حالة الاشتراك والرصيد
- إنشاء المشتريات
- إنشاء أكواد الإحالة
- صلاحيات المدير
- توليد الخطة عبر Claude API

جميع الاختبارات ناجحة ✅
